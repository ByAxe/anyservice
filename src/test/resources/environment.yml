spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/anyservice_test_db?currentSchema=anyservice_test
    username: postgres
    password: postgres
